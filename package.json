{
  "name": "vibe-walk-scripts",
  "version": "0.0.1",
  "private": true,
  "description": "Vibe-Walk plugin scripts — anchor codemod (M4) and supporting toolchain",
  "scripts": {
    "test:js": "node ./node_modules/jest-cli/bin/jest.js --testPathPattern=\"plugins/vibe-walk/scripts/anchors/__tests__/inject_anchors.test\" --forceExit",
    "test:py": "python -m pytest tests/ -q",
    "test": "npm run test:py && npm run test:js"
  },
  "devDependencies": {
    "jscodeshift": "^17.3.0",
    "jest": "^29.7.0",
    "jest-environment-node": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {}
  }
}
